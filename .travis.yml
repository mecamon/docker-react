language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t mecamon/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mecamon/docker-react npm run test